v0.4.0 — tests, bug fixes, clippy cleanup

Changes:
- Rust unit tests (52): config, mining, keypair, logger, rpc, ws_client
- Frontend tests (30): helpers, mining, UI via Vitest + jsdom
- Critical bugfix: wallet_list_accounts now uses stored logged-in client
- Critical bugfix: shared reqwest::Client in AppState for connection pooling
- Critical bugfix: std::sync::Mutex in WsCmdSender (no blocking_lock in async)
- Critical bugfix: waitForWsResult handles ws-error events
- Config: #[serde(default)] on all PotOConfig fields
- Config: ws_url handles trailing slashes in rpc_url
- Logger: YYYY-MM-DD HH:MM:SS.mmm timestamp format
- Mining: F64/I64 tensor data fallback
- Keypair: simplified generation
- Removed unused url dependency
- clippy: zero warnings
